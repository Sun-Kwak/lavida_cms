{
    "sourceFile": "SIMPLE_SOLUTION.md",
    "activeCommit": 0,
    "commits": [
        {
            "activePatchIndex": 0,
            "patches": [
                {
                    "date": 1763878962465,
                    "content": "Index: \n===================================================================\n--- \n+++ \n"
                }
            ],
            "date": 1763878962465,
            "name": "Commit-0",
            "content": "# 휴일 설정 단순화 - 간단한 해결책\n\n## 현재 상황\n- StaffRegister, Reservation 등에서 요일 기반 복잡한 로직 사용\n- WeekSchedule 타입으로 월~일 요일별 근무시간/휴일 관리\n- 코드가 매우 복잡하고 버그 발생 가능성 높음\n\n## 즉시 적용 가능한 해결책\n\n### 1단계: DB는 이미 준비됨 ✅\n- Staff.holidays: string[] 필드 존재\n- StaffService에 CRUD 메서드 모두 구현됨\n- HolidayManagement 컴포넌트 완성됨\n\n### 2단계: 기존 코드는 그대로 두고, 새로운 방식 추가\n\n#### 방법 A: 병행 사용\n```typescript\n// 기존 코드는 유지\n// 새로운 휴일 체크 추가\nconst isHoliday = await dbManager.isStaffHoliday(staffId, dateString);\nif (isHoliday) {\n  // 휴일 처리\n}\n```\n\n#### 방법 B: StaffRegister에 안내 추가\n직원 등록 폼에서 복잡한 주간 휴일 설정 UI를 제거하고:\n\n```tsx\n{formData.program && (\n  <div className=\"holiday-notice\">\n    <h4>📅 휴일 설정 안내</h4>\n    <p>\n      직원 등록 후 <strong>'직원 관리 → 휴일 관리'</strong> 메뉴에서 \n      캘린더를 통해 간편하게 휴일을 설정할 수 있습니다.\n    </p>\n  </div>\n)}\n```\n\n그리고 직원 등록 후:\n```tsx\n{newlyCreatedStaffId && (\n  <>\n    <div className=\"success-message\">\n      ✅ 직원이 성공적으로 등록되었습니다!\n    </div>\n    <HolidayManagement staffId={newlyCreatedStaffId} />\n  </>\n)}\n```\n\n### 3단계: 점진적 마이그레이션\n1. 새로 등록되는 직원: Staff.holidays 사용\n2. 기존 직원: 필요시 HolidayManagement에서 설정\n3. 예약/스케줄: `isStaffHoliday()` 우선 체크, 없으면 기존 로직 사용\n\n## 구현 코드 (최소 수정)\n\n### StaffRegister.tsx 수정사항\n\n#### 1. 상태 추가\n```typescript\nconst [newlyCreatedStaffId, setNewlyCreatedStaffId] = useState<string | null>(null);\nconst [showHolidayManagement, setShowHolidayManagement] = useState(false);\n```\n\n#### 2. handleSubmit 수정\n```typescript\nconst savedStaff = await dbManager.addStaff(staffData);\nsetNewlyCreatedStaffId(savedStaff.id);\nsetShowHolidayManagement(true);\ntoast.success('직원이 등록되었습니다! 이제 휴일을 설정하세요.');\n\n// 기존 weeklyHolidayData 저장 로직은 제거\n```\n\n#### 3. UI에 간단한 안내 추가\n```tsx\n{formData.program && !showHolidayManagement && (\n  <div style={{padding: '20px', background: '#f0f7ff', borderRadius: '8px'}}>\n    <strong>💡 휴일 설정 안내</strong>\n    <p>직원 등록 후 캘린더에서 휴일을 설정할 수 있습니다.</p>\n  </div>\n)}\n\n{showHolidayManagement && newlyCreatedStaffId && (\n  <HolidayManagement \n    staffId={newlyCreatedStaffId}\n    onHolidayChange={() => {\n      toast.success('휴일 설정이 저장되었습니다.');\n    }}\n  />\n)}\n```\n\n### Reservation 수정사항\n\n#### 기존 체크 로직에 추가\n```typescript\n// 예약 가능 여부 체크\nconst checkStaffAvailability = async (staffId: string, date: Date) => {\n  const dateString = date.toISOString().split('T')[0];\n  \n  // 새로운 방식: Staff.holidays 체크\n  const isHoliday = await dbManager.isStaffHoliday(staffId, dateString);\n  if (isHoliday) {\n    return false; // 휴일\n  }\n  \n  // 기존 로직도 유지 (하위 호환성)\n  // ... existing code ...\n  \n  return true;\n};\n```\n\n## 장점\n1. **기존 코드 최소 수정**: 큰 리팩토링 없이 점진적 개선\n2. **즉시 사용 가능**: DB와 UI 컴포넌트는 이미 준비됨\n3. **사용자 친화적**: 캘린더 기반 직관적 UI\n4. **하위 호환성**: 기존 로직과 병행 운영 가능\n\n## 결론\n**복잡한 전면 리팩토링보다는, 새로운 간단한 방식을 추가하고 점진적으로 전환하는 것을 추천합니다.**\n\n이 방식으로:\n- ✅ 요일 관련 복잡한 로직 문제 해결\n- ✅ 날짜 기반으로 단순화\n- ✅ 기존 코드와 충돌 없음\n- ✅ 사용자 경험 개선\n"
        }
    ]
}