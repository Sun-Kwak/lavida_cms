{
    "sourceFile": "src/pages/CMS/Settings/ReferralPointTable.tsx",
    "activeCommit": 0,
    "commits": [
        {
            "activePatchIndex": 8,
            "patches": [
                {
                    "date": 1763550103659,
                    "content": "Index: \n===================================================================\n--- \n+++ \n"
                },
                {
                    "date": 1763550149790,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -195,8 +195,18 @@\n   color: ${AppColors.onSurface};\n   font-size: ${AppTextStyles.body2.fontSize};\n `;\n \n+const InfoMessage = styled.div`\n+  padding: 12px 16px;\n+  margin-bottom: 16px;\n+  border-radius: 8px;\n+  background-color: ${AppColors.primary}10;\n+  color: ${AppColors.onSurface};\n+  font-size: ${AppTextStyles.body2.fontSize};\n+  line-height: 1.5;\n+`;\n+\n const AddForm = styled.div`\n   padding: 16px;\n   border: 1px solid ${AppColors.borderLight};\n   border-radius: 8px;\n@@ -558,8 +568,15 @@\n           </RefreshButton>\n         </ErrorState>\n       )}\n \n+      {/* 권한 안내 메시지 */}\n+      {!canAdd() && (\n+        <InfoMessage>\n+          ℹ️ 추천 포인트 설정 조회만 가능합니다. 추가/수정/삭제는 MASTER 또는 EDITOR 권한이 필요합니다.\n+        </InfoMessage>\n+      )}\n+\n       {isAdding && (\n         <AddForm>\n           <FormRow>\n             <Select\n"
                },
                {
                    "date": 1763550288895,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -322,14 +322,32 @@\n   const [currentUserBranchId, setCurrentUserBranchId] = useState<string>('');\n \n   // 현재 사용자 정보 로드\n   useEffect(() => {\n-    const userInfo = sessionStorage.getItem('currentUser');\n-    if (userInfo) {\n-      const user = JSON.parse(userInfo);\n-      setCurrentUserPermission(user.permission || 'VIEWER');\n-      setCurrentUserBranchId(user.branchId || '');\n-    }\n+    const loadCurrentUser = async () => {\n+      const adminId = sessionStorage.getItem('adminId');\n+      console.log('🔍 ReferralPointTable - adminId:', adminId);\n+      \n+      if (adminId) {\n+        // 전체 직원 목록에서 현재 사용자 찾기\n+        const allStaff = await dbManager.getAllStaff();\n+        const currentUser = allStaff.find(staff => staff.loginId === adminId);\n+        \n+        console.log('👤 현재 사용자:', currentUser);\n+        \n+        if (currentUser) {\n+          console.log('🔑 권한:', currentUser.permission);\n+          setCurrentUserPermission(currentUser.permission || 'VIEWER');\n+          setCurrentUserBranchId(currentUser.branchId || '');\n+        } else {\n+          console.warn('⚠️ 사용자를 찾을 수 없습니다');\n+        }\n+      } else {\n+        console.warn('⚠️ adminId가 없습니다');\n+      }\n+    };\n+    \n+    loadCurrentUser();\n   }, []);\n \n   // 컴포넌트 마운트 시 데이터 로드\n   useEffect(() => {\n@@ -359,8 +377,9 @@\n   };\n \n   // 권한 체크 함수들\n   const canAdd = (): boolean => {\n+    console.log('🔍 canAdd 체크 - 현재 권한:', currentUserPermission);\n     if (currentUserPermission === 'MASTER') return true;\n     if (currentUserPermission === 'EDITOR') return true;\n     return false;\n   };\n@@ -575,8 +594,13 @@\n           ℹ️ 추천 포인트 설정 조회만 가능합니다. 추가/수정/삭제는 MASTER 또는 EDITOR 권한이 필요합니다.\n         </InfoMessage>\n       )}\n \n+      {/* 디버그 정보 */}\n+      <InfoMessage style={{ backgroundColor: '#f0f0f0', fontSize: '12px' }}>\n+        🔍 디버그: 권한={currentUserPermission} | canAdd={canAdd() ? 'true' : 'false'} | 사용가능지점={availableBranches.length}개\n+      </InfoMessage>\n+\n       {isAdding && (\n         <AddForm>\n           <FormRow>\n             <Select\n"
                },
                {
                    "date": 1763550326637,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -324,26 +324,18 @@\n   // 현재 사용자 정보 로드\n   useEffect(() => {\n     const loadCurrentUser = async () => {\n       const adminId = sessionStorage.getItem('adminId');\n-      console.log('🔍 ReferralPointTable - adminId:', adminId);\n       \n       if (adminId) {\n         // 전체 직원 목록에서 현재 사용자 찾기\n         const allStaff = await dbManager.getAllStaff();\n         const currentUser = allStaff.find(staff => staff.loginId === adminId);\n         \n-        console.log('👤 현재 사용자:', currentUser);\n-        \n         if (currentUser) {\n-          console.log('🔑 권한:', currentUser.permission);\n           setCurrentUserPermission(currentUser.permission || 'VIEWER');\n           setCurrentUserBranchId(currentUser.branchId || '');\n-        } else {\n-          console.warn('⚠️ 사용자를 찾을 수 없습니다');\n         }\n-      } else {\n-        console.warn('⚠️ adminId가 없습니다');\n       }\n     };\n     \n     loadCurrentUser();\n@@ -594,13 +586,8 @@\n           ℹ️ 추천 포인트 설정 조회만 가능합니다. 추가/수정/삭제는 MASTER 또는 EDITOR 권한이 필요합니다.\n         </InfoMessage>\n       )}\n \n-      {/* 디버그 정보 */}\n-      <InfoMessage style={{ backgroundColor: '#f0f0f0', fontSize: '12px' }}>\n-        🔍 디버그: 권한={currentUserPermission} | canAdd={canAdd() ? 'true' : 'false'} | 사용가능지점={availableBranches.length}개\n-      </InfoMessage>\n-\n       {isAdding && (\n         <AddForm>\n           <FormRow>\n             <Select\n"
                },
                {
                    "date": 1763550448488,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -2,8 +2,10 @@\n import styled from 'styled-components';\n import { AppColors } from '../../../styles/colors';\n import { AppTextStyles } from '../../../styles/textStyles';\n import { dbManager, ReferralPointSettings, Branch } from '../../../utils/indexedDB';\n+import { AppDropdownField, DropdownOption } from '../../../customComponents/AppDropdownField';\n+import { AppNumberTextField } from '../../../customComponents/AppNumberTextField';\n \n const Container = styled.div`\n   width: 100%;\n `;\n@@ -211,54 +213,23 @@\n   border: 1px solid ${AppColors.borderLight};\n   border-radius: 8px;\n   margin-bottom: 16px;\n   background-color: ${AppColors.background};\n+  display: flex;\n+  flex-direction: column;\n+  gap: 16px;\n `;\n \n const FormRow = styled.div`\n-  display: flex;\n-  gap: 12px;\n-  margin-bottom: 12px;\n+  display: grid;\n+  grid-template-columns: 1fr 1fr;\n+  gap: 16px;\n `;\n \n-const Select = styled.select`\n-  flex: 1;\n-  padding: 8px 12px;\n-  border: 1px solid ${AppColors.borderLight};\n-  border-radius: 4px;\n-  font-size: ${AppTextStyles.body2.fontSize};\n-  box-sizing: border-box;\n \n-  &:focus {\n-    outline: none;\n-    border-color: ${AppColors.primary};\n-  }\n \n-  &:disabled {\n-    background-color: ${AppColors.background};\n-    opacity: 0.5;\n-  }\n-`;\n \n-const Input = styled.input`\n-  flex: 1;\n-  padding: 8px 12px;\n-  border: 1px solid ${AppColors.borderLight};\n-  border-radius: 4px;\n-  font-size: ${AppTextStyles.body2.fontSize};\n-  box-sizing: border-box;\n \n-  &:focus {\n-    outline: none;\n-    border-color: ${AppColors.primary};\n-  }\n-\n-  &:disabled {\n-    background-color: ${AppColors.background};\n-    opacity: 0.5;\n-  }\n-`;\n-\n const FormButtons = styled.div`\n   display: flex;\n   gap: 8px;\n `;\n@@ -369,9 +340,8 @@\n   };\n \n   // 권한 체크 함수들\n   const canAdd = (): boolean => {\n-    console.log('🔍 canAdd 체크 - 현재 권한:', currentUserPermission);\n     if (currentUserPermission === 'MASTER') return true;\n     if (currentUserPermission === 'EDITOR') return true;\n     return false;\n   };\n@@ -588,39 +558,37 @@\n       )}\n \n       {isAdding && (\n         <AddForm>\n+          <AppDropdownField\n+            value={selectedBranchId}\n+            onChange={(value) => setSelectedBranchId(value)}\n+            options={availableBranches.map(branch => ({\n+              value: branch.id,\n+              label: branch.name,\n+            }))}\n+            label=\"지점 선택\"\n+            placeholder=\"지점을 선택해주세요\"\n+            readOnly={saving}\n+          />\n           <FormRow>\n-            <Select\n-              value={selectedBranchId}\n-              onChange={(e) => setSelectedBranchId(e.target.value)}\n-              disabled={saving}\n-              autoFocus\n-            >\n-              <option value=\"\">지점 선택 (필수)</option>\n-              {availableBranches.map(branch => (\n-                <option key={branch.id} value={branch.id}>\n-                  {branch.name}\n-                </option>\n-              ))}\n-            </Select>\n-          </FormRow>\n-          <FormRow>\n-            <Input\n-              type=\"number\"\n-              placeholder=\"추천한 사람 포인트\"\n+            <AppNumberTextField\n               value={referrerPoints}\n               onChange={(e) => setReferrerPoints(e.target.value)}\n-              disabled={saving}\n-              min=\"0\"\n+              label=\"추천한 사람 포인트\"\n+              placeholder=\"포인트 입력\"\n+              minValue={0}\n+              unit=\"P\"\n+              readOnly={saving}\n             />\n-            <Input\n-              type=\"number\"\n-              placeholder=\"추천받은 사람 포인트\"\n+            <AppNumberTextField\n               value={referredPoints}\n               onChange={(e) => setReferredPoints(e.target.value)}\n-              disabled={saving}\n-              min=\"0\"\n+              label=\"추천받은 사람 포인트\"\n+              placeholder=\"포인트 입력\"\n+              minValue={0}\n+              unit=\"P\"\n+              readOnly={saving}\n             />\n           </FormRow>\n           <FormButtons>\n             <SaveButton onClick={handleAddSettings} disabled={saving}>\n@@ -672,34 +640,41 @@\n                 <div style={{ \n                   width: '100%', \n                   display: 'flex', \n                   flexDirection: 'column', \n-                  gap: '12px',\n+                  gap: '16px',\n                   padding: '8px 0'\n                 }}>\n                   <div style={{ \n                     fontWeight: 500, \n-                    marginBottom: '8px',\n-                    color: AppColors.onSurface \n+                    marginBottom: '4px',\n+                    color: AppColors.onSurface,\n+                    fontSize: '16px'\n                   }}>\n                     {setting.branchName}\n                   </div>\n-                  <div style={{ display: 'flex', gap: '8px' }}>\n-                    <Input\n-                      type=\"number\"\n+                  <div style={{ \n+                    display: 'grid', \n+                    gridTemplateColumns: '1fr 1fr',\n+                    gap: '16px' \n+                  }}>\n+                    <AppNumberTextField\n                       value={editReferrerPoints}\n                       onChange={(e) => setEditReferrerPoints(e.target.value)}\n-                      disabled={saving}\n-                      placeholder=\"추천한 사람 포인트\"\n-                      min=\"0\"\n+                      label=\"추천한 사람 포인트\"\n+                      placeholder=\"포인트 입력\"\n+                      minValue={0}\n+                      unit=\"P\"\n+                      readOnly={saving}\n                     />\n-                    <Input\n-                      type=\"number\"\n+                    <AppNumberTextField\n                       value={editReferredPoints}\n                       onChange={(e) => setEditReferredPoints(e.target.value)}\n-                      disabled={saving}\n-                      placeholder=\"추천받은 사람 포인트\"\n-                      min=\"0\"\n+                      label=\"추천받은 사람 포인트\"\n+                      placeholder=\"포인트 입력\"\n+                      minValue={0}\n+                      unit=\"P\"\n+                      readOnly={saving}\n                     />\n                   </div>\n                   <div style={{ \n                     display: 'flex', \n"
                },
                {
                    "date": 1763550497980,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -357,12 +357,14 @@\n     if (currentUserPermission === 'EDITOR' && setting.branchId === currentUserBranchId) return true;\n     return false;\n   };\n \n-  // 사용 가능한 지점 목록 (이미 설정된 지점 제외)\n+  // 사용 가능한 지점 목록 (이미 설정된 지점 제외, \"전체\" 지점 제외)\n   const getAvailableBranches = (): Branch[] => {\n     const usedBranchIds = new Set(settings.map(s => s.branchId));\n-    let availableBranches = branches.filter(b => !usedBranchIds.has(b.id));\n+    let availableBranches = branches.filter(b => \n+      !usedBranchIds.has(b.id) && b.name !== '전체' // \"전체\" 지점 제외\n+    );\n     \n     // EDITOR 권한은 본인 지점만\n     if (currentUserPermission === 'EDITOR') {\n       availableBranches = availableBranches.filter(b => b.id === currentUserBranchId);\n@@ -556,8 +558,15 @@\n           ℹ️ 추천 포인트 설정 조회만 가능합니다. 추가/수정/삭제는 MASTER 또는 EDITOR 권한이 필요합니다.\n         </InfoMessage>\n       )}\n \n+      {/* \"전체\" 지점 기본값 안내 */}\n+      {canAdd() && (\n+        <InfoMessage>\n+          💡 \"전체\" 지점은 개별 설정이 없는 지점의 기본값으로 사용됩니다. 개별 지점 설정이 있으면 해당 설정이 우선 적용됩니다.\n+        </InfoMessage>\n+      )}\n+\n       {isAdding && (\n         <AddForm>\n           <AppDropdownField\n             value={selectedBranchId}\n"
                },
                {
                    "date": 1763550639237,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -116,10 +116,19 @@\n   flex-direction: column;\n   gap: 4px;\n `;\n \n-const BranchName = styled.span`\n+const BranchName = styled.span<{ isDefault?: boolean }>`\n   font-weight: 500;\n+  color: ${props => props.isDefault ? AppColors.primary : 'inherit'};\n+  ${props => props.isDefault && `\n+    &::after {\n+      content: ' (기본값)';\n+      font-size: ${AppTextStyles.label3.fontSize};\n+      color: ${AppColors.primary};\n+      font-weight: 400;\n+    }\n+  `}\n `;\n \n const PointDetails = styled.div`\n   display: flex;\n@@ -346,14 +355,20 @@\n     return false;\n   };\n \n   const canEdit = (setting: ReferralPointSettings): boolean => {\n+    // \"전체\" 지점은 수정 불가\n+    if (setting.branchName === '전체') return false;\n+    \n     if (currentUserPermission === 'MASTER') return true;\n     if (currentUserPermission === 'EDITOR' && setting.branchId === currentUserBranchId) return true;\n     return false;\n   };\n \n   const canDelete = (setting: ReferralPointSettings): boolean => {\n+    // \"전체\" 지점은 삭제 불가\n+    if (setting.branchName === '전체') return false;\n+    \n     if (currentUserPermission === 'MASTER') return true;\n     if (currentUserPermission === 'EDITOR' && setting.branchId === currentUserBranchId) return true;\n     return false;\n   };\n@@ -561,9 +576,9 @@\n \n       {/* \"전체\" 지점 기본값 안내 */}\n       {canAdd() && (\n         <InfoMessage>\n-          💡 \"전체\" 지점은 개별 설정이 없는 지점의 기본값으로 사용됩니다. 개별 지점 설정이 있으면 해당 설정이 우선 적용됩니다.\n+          💡 \"전체\" 지점은 기본값으로, 개별 지점 설정이 없을 때 적용됩니다. 개별 지점은 자유롭게 추가/수정/삭제할 수 있습니다.\n         </InfoMessage>\n       )}\n \n       {isAdding && (\n@@ -718,9 +733,9 @@\n               ) : (\n                 // 보기 모드\n                 <>\n                   <SettingInfo>\n-                    <BranchName>{setting.branchName}</BranchName>\n+                    <BranchName isDefault={setting.branchName === '전체'}>{setting.branchName}</BranchName>\n                     <PointDetails>\n                       <PointItem>\n                         <PointLabel>추천한 사람:</PointLabel>\n                         <PointValue>{setting.referrerPoints.toLocaleString()}P</PointValue>\n"
                },
                {
                    "date": 1763550700807,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -375,10 +375,11 @@\n \n   // 사용 가능한 지점 목록 (이미 설정된 지점 제외, \"전체\" 지점 제외)\n   const getAvailableBranches = (): Branch[] => {\n     const usedBranchIds = new Set(settings.map(s => s.branchId));\n+    // \"전체\" 지점은 드롭다운에서 제외 (기본값이므로 개별 추가 불가)\n     let availableBranches = branches.filter(b => \n-      !usedBranchIds.has(b.id) && b.name !== '전체' // \"전체\" 지점 제외\n+      !usedBranchIds.has(b.id) && b.name !== '전체'\n     );\n     \n     // EDITOR 권한은 본인 지점만\n     if (currentUserPermission === 'EDITOR') {\n"
                },
                {
                    "date": 1763550924046,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -628,16 +628,17 @@\n \n       {!isAdding && canAdd() && (\n         <AddButton \n           onClick={() => {\n-            if (availableBranches.length === 0) {\n+            const available = getAvailableBranches();\n+            if (available.length === 0) {\n               alert('추가 가능한 지점이 없습니다. 모든 지점에 이미 설정이 등록되어 있습니다.');\n               return;\n             }\n             setIsAdding(true);\n             setEditingId(null);\n           }} \n-          disabled={loading || editingId !== null || availableBranches.length === 0}\n+          disabled={loading || editingId !== null}\n         >\n           + 추천 포인트 설정 추가\n         </AddButton>\n       )}\n"
                }
            ],
            "date": 1763550103659,
            "name": "Commit-0",
            "content": "import React, { useState, useEffect } from 'react';\nimport styled from 'styled-components';\nimport { AppColors } from '../../../styles/colors';\nimport { AppTextStyles } from '../../../styles/textStyles';\nimport { dbManager, ReferralPointSettings, Branch } from '../../../utils/indexedDB';\n\nconst Container = styled.div`\n  width: 100%;\n`;\n\nconst LoadingState = styled.div`\n  padding: 24px;\n  text-align: center;\n  color: ${AppColors.onSurface};\n  font-size: ${AppTextStyles.body2.fontSize};\n`;\n\nconst ErrorState = styled.div`\n  padding: 16px;\n  margin-bottom: 16px;\n  border: 1px solid ${AppColors.error};\n  border-radius: 8px;\n  background-color: ${AppColors.error}10;\n  color: ${AppColors.error};\n  font-size: ${AppTextStyles.body2.fontSize};\n`;\n\nconst RefreshButton = styled.button`\n  padding: 8px 16px;\n  margin-left: 8px;\n  border: 1px solid ${AppColors.primary};\n  border-radius: 4px;\n  background-color: transparent;\n  color: ${AppColors.primary};\n  font-size: ${AppTextStyles.label3.fontSize};\n  cursor: pointer;\n  transition: all 0.2s ease;\n\n  &:hover {\n    background-color: ${AppColors.primary};\n    color: white;\n  }\n`;\n\nconst AddButton = styled.button`\n  width: 100%;\n  padding: 12px;\n  margin-bottom: 16px;\n  border: 2px dashed ${AppColors.borderLight};\n  border-radius: 8px;\n  background-color: transparent;\n  color: ${AppColors.onSurface};\n  font-size: ${AppTextStyles.body2.fontSize};\n  cursor: pointer;\n  transition: all 0.2s ease;\n\n  &:hover:not(:disabled) {\n    border-color: ${AppColors.primary};\n    color: ${AppColors.primary};\n    background-color: ${AppColors.primary}05;\n  }\n\n  &:disabled {\n    opacity: 0.5;\n    cursor: not-allowed;\n  }\n`;\n\nconst Table = styled.div`\n  border: 1px solid ${AppColors.borderLight};\n  border-radius: 8px;\n  overflow: hidden;\n`;\n\nconst TableHeader = styled.div`\n  background-color: ${AppColors.background};\n  padding: 12px 16px;\n  border-bottom: 1px solid ${AppColors.borderLight};\n  font-weight: 600;\n  font-size: ${AppTextStyles.body2.fontSize};\n  color: ${AppColors.onSurface};\n  display: flex;\n  justify-content: space-between;\n  align-items: center;\n`;\n\nconst TableStats = styled.span`\n  font-size: ${AppTextStyles.label3.fontSize};\n  color: ${AppColors.onSurface}80;\n  font-weight: normal;\n`;\n\nconst TableRow = styled.div`\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  padding: 12px 16px;\n  border-bottom: 1px solid ${AppColors.borderLight};\n  font-size: ${AppTextStyles.body2.fontSize};\n  color: ${AppColors.onSurface};\n\n  &:last-child {\n    border-bottom: none;\n  }\n\n  &:hover {\n    background-color: ${AppColors.background};\n  }\n`;\n\nconst SettingInfo = styled.div`\n  flex: 1;\n  display: flex;\n  flex-direction: column;\n  gap: 4px;\n`;\n\nconst BranchName = styled.span`\n  font-weight: 500;\n`;\n\nconst PointDetails = styled.div`\n  display: flex;\n  gap: 12px;\n  font-size: ${AppTextStyles.label3.fontSize};\n  color: ${AppColors.onSurface}60;\n`;\n\nconst PointItem = styled.div`\n  display: flex;\n  align-items: center;\n  gap: 4px;\n`;\n\nconst PointLabel = styled.span`\n  font-weight: 500;\n  color: ${AppColors.onSurface}80;\n`;\n\nconst PointValue = styled.span`\n  color: ${AppColors.primary};\n  font-weight: 600;\n`;\n\nconst ActionButtons = styled.div`\n  display: flex;\n  gap: 8px;\n`;\n\nconst EditButton = styled.button`\n  padding: 4px 8px;\n  border: 1px solid ${AppColors.primary};\n  border-radius: 4px;\n  background-color: transparent;\n  color: ${AppColors.primary};\n  font-size: ${AppTextStyles.label3.fontSize};\n  cursor: pointer;\n  transition: all 0.2s ease;\n\n  &:hover:not(:disabled) {\n    background-color: ${AppColors.primary};\n    color: white;\n  }\n\n  &:disabled {\n    opacity: 0.5;\n    cursor: not-allowed;\n  }\n`;\n\nconst DeleteButton = styled.button`\n  padding: 4px 8px;\n  border: 1px solid ${AppColors.error};\n  border-radius: 4px;\n  background-color: transparent;\n  color: ${AppColors.error};\n  font-size: ${AppTextStyles.label3.fontSize};\n  cursor: pointer;\n  transition: all 0.2s ease;\n\n  &:hover:not(:disabled) {\n    background-color: ${AppColors.error};\n    color: white;\n  }\n\n  &:disabled {\n    opacity: 0.5;\n    cursor: not-allowed;\n  }\n`;\n\nconst EmptyState = styled.div`\n  padding: 24px;\n  text-align: center;\n  color: ${AppColors.onSurface};\n  font-size: ${AppTextStyles.body2.fontSize};\n`;\n\nconst AddForm = styled.div`\n  padding: 16px;\n  border: 1px solid ${AppColors.borderLight};\n  border-radius: 8px;\n  margin-bottom: 16px;\n  background-color: ${AppColors.background};\n`;\n\nconst FormRow = styled.div`\n  display: flex;\n  gap: 12px;\n  margin-bottom: 12px;\n`;\n\nconst Select = styled.select`\n  flex: 1;\n  padding: 8px 12px;\n  border: 1px solid ${AppColors.borderLight};\n  border-radius: 4px;\n  font-size: ${AppTextStyles.body2.fontSize};\n  box-sizing: border-box;\n\n  &:focus {\n    outline: none;\n    border-color: ${AppColors.primary};\n  }\n\n  &:disabled {\n    background-color: ${AppColors.background};\n    opacity: 0.5;\n  }\n`;\n\nconst Input = styled.input`\n  flex: 1;\n  padding: 8px 12px;\n  border: 1px solid ${AppColors.borderLight};\n  border-radius: 4px;\n  font-size: ${AppTextStyles.body2.fontSize};\n  box-sizing: border-box;\n\n  &:focus {\n    outline: none;\n    border-color: ${AppColors.primary};\n  }\n\n  &:disabled {\n    background-color: ${AppColors.background};\n    opacity: 0.5;\n  }\n`;\n\nconst FormButtons = styled.div`\n  display: flex;\n  gap: 8px;\n`;\n\nconst SaveButton = styled.button`\n  flex: 1;\n  padding: 8px 16px;\n  border: none;\n  border-radius: 4px;\n  background-color: ${AppColors.primary};\n  color: white;\n  font-size: ${AppTextStyles.body2.fontSize};\n  cursor: pointer;\n  transition: all 0.2s ease;\n\n  &:hover:not(:disabled) {\n    background-color: ${AppColors.secondary};\n  }\n\n  &:disabled {\n    opacity: 0.5;\n    cursor: not-allowed;\n  }\n`;\n\nconst CancelButton = styled.button`\n  flex: 1;\n  padding: 8px 16px;\n  border: 1px solid ${AppColors.borderLight};\n  border-radius: 4px;\n  background-color: transparent;\n  color: ${AppColors.onSurface};\n  font-size: ${AppTextStyles.body2.fontSize};\n  cursor: pointer;\n  transition: all 0.2s ease;\n\n  &:hover:not(:disabled) {\n    background-color: ${AppColors.background};\n  }\n\n  &:disabled {\n    opacity: 0.5;\n    cursor: not-allowed;\n  }\n`;\n\nconst ReferralPointTable: React.FC = () => {\n  const [settings, setSettings] = useState<ReferralPointSettings[]>([]);\n  const [branches, setBranches] = useState<Branch[]>([]);\n  const [isAdding, setIsAdding] = useState(false);\n  const [editingId, setEditingId] = useState<string | null>(null);\n  const [selectedBranchId, setSelectedBranchId] = useState('');\n  const [referrerPoints, setReferrerPoints] = useState('');\n  const [referredPoints, setReferredPoints] = useState('');\n  const [editReferrerPoints, setEditReferrerPoints] = useState('');\n  const [editReferredPoints, setEditReferredPoints] = useState('');\n  const [loading, setLoading] = useState(true);\n  const [saving, setSaving] = useState(false);\n  const [error, setError] = useState<string | null>(null);\n  const [currentUserPermission, setCurrentUserPermission] = useState<string>('VIEWER');\n  const [currentUserBranchId, setCurrentUserBranchId] = useState<string>('');\n\n  // 현재 사용자 정보 로드\n  useEffect(() => {\n    const userInfo = sessionStorage.getItem('currentUser');\n    if (userInfo) {\n      const user = JSON.parse(userInfo);\n      setCurrentUserPermission(user.permission || 'VIEWER');\n      setCurrentUserBranchId(user.branchId || '');\n    }\n  }, []);\n\n  // 컴포넌트 마운트 시 데이터 로드\n  useEffect(() => {\n    loadData();\n  }, []);\n\n  const loadData = async () => {\n    try {\n      setLoading(true);\n      setError(null);\n      \n      const [settingsData, branchesData] = await Promise.all([\n        dbManager.referralPoint.getAllReferralPointSettings(),\n        dbManager.getAllBranches(),\n      ]);\n      \n      setSettings(settingsData.sort((a, b) => \n        new Date(a.createdAt).getTime() - new Date(b.createdAt).getTime()\n      ));\n      setBranches(branchesData);\n    } catch (err) {\n      console.error('데이터 로드 실패:', err);\n      setError('데이터를 불러오는데 실패했습니다.');\n    } finally {\n      setLoading(false);\n    }\n  };\n\n  // 권한 체크 함수들\n  const canAdd = (): boolean => {\n    if (currentUserPermission === 'MASTER') return true;\n    if (currentUserPermission === 'EDITOR') return true;\n    return false;\n  };\n\n  const canEdit = (setting: ReferralPointSettings): boolean => {\n    if (currentUserPermission === 'MASTER') return true;\n    if (currentUserPermission === 'EDITOR' && setting.branchId === currentUserBranchId) return true;\n    return false;\n  };\n\n  const canDelete = (setting: ReferralPointSettings): boolean => {\n    if (currentUserPermission === 'MASTER') return true;\n    if (currentUserPermission === 'EDITOR' && setting.branchId === currentUserBranchId) return true;\n    return false;\n  };\n\n  // 사용 가능한 지점 목록 (이미 설정된 지점 제외)\n  const getAvailableBranches = (): Branch[] => {\n    const usedBranchIds = new Set(settings.map(s => s.branchId));\n    let availableBranches = branches.filter(b => !usedBranchIds.has(b.id));\n    \n    // EDITOR 권한은 본인 지점만\n    if (currentUserPermission === 'EDITOR') {\n      availableBranches = availableBranches.filter(b => b.id === currentUserBranchId);\n    }\n    \n    return availableBranches;\n  };\n\n  const handleAddSettings = async () => {\n    if (!selectedBranchId) {\n      alert('지점을 선택해주세요.');\n      return;\n    }\n\n    const referrerPointsNum = parseInt(referrerPoints);\n    const referredPointsNum = parseInt(referredPoints);\n\n    if (isNaN(referrerPointsNum) || referrerPointsNum < 0) {\n      alert('추천한 사람 포인트를 올바르게 입력해주세요.');\n      return;\n    }\n\n    if (isNaN(referredPointsNum) || referredPointsNum < 0) {\n      alert('추천받은 사람 포인트를 올바르게 입력해주세요.');\n      return;\n    }\n\n    try {\n      setSaving(true);\n      setError(null);\n\n      const branch = branches.find(b => b.id === selectedBranchId);\n      if (!branch) {\n        throw new Error('지점을 찾을 수 없습니다.');\n      }\n\n      const newSettings = await dbManager.referralPoint.addReferralPointSettings({\n        branchId: selectedBranchId,\n        branchName: branch.name,\n        referrerPoints: referrerPointsNum,\n        referredPoints: referredPointsNum,\n        isActive: true,\n      });\n\n      setSettings(prev => {\n        const updated = [...prev, newSettings];\n        return updated.sort((a, b) => \n          new Date(a.createdAt).getTime() - new Date(b.createdAt).getTime()\n        );\n      });\n\n      setSelectedBranchId('');\n      setReferrerPoints('');\n      setReferredPoints('');\n      setIsAdding(false);\n      setEditingId(null);\n\n      console.log('추천 포인트 설정 추가됨:', newSettings);\n    } catch (err) {\n      console.error('추천 포인트 설정 추가 실패:', err);\n      setError(err instanceof Error ? err.message : '추천 포인트 설정 추가에 실패했습니다.');\n    } finally {\n      setSaving(false);\n    }\n  };\n\n  const handleDeleteSettings = async (id: string) => {\n    const settingToDelete = settings.find(s => s.id === id);\n    if (!settingToDelete || !canDelete(settingToDelete)) {\n      alert('삭제 권한이 없습니다.');\n      return;\n    }\n\n    if (!window.confirm('정말로 삭제하시겠습니까?')) {\n      return;\n    }\n\n    try {\n      setError(null);\n      const success = await dbManager.referralPoint.deleteReferralPointSettings(id);\n\n      if (success) {\n        setSettings(prev => prev.filter(s => s.id !== id));\n        console.log('추천 포인트 설정 삭제됨:', id);\n      } else {\n        throw new Error('삭제 실패');\n      }\n    } catch (err) {\n      console.error('추천 포인트 설정 삭제 실패:', err);\n      setError('추천 포인트 설정 삭제에 실패했습니다.');\n    }\n  };\n\n  const handleEditSettings = (setting: ReferralPointSettings) => {\n    if (!canEdit(setting)) {\n      alert('수정 권한이 없습니다.');\n      return;\n    }\n\n    setEditingId(setting.id);\n    setEditReferrerPoints(setting.referrerPoints.toString());\n    setEditReferredPoints(setting.referredPoints.toString());\n    setIsAdding(false);\n  };\n\n  const handleUpdateSettings = async () => {\n    if (!editingId) return;\n\n    const referrerPointsNum = parseInt(editReferrerPoints);\n    const referredPointsNum = parseInt(editReferredPoints);\n\n    if (isNaN(referrerPointsNum) || referrerPointsNum < 0) {\n      alert('추천한 사람 포인트를 올바르게 입력해주세요.');\n      return;\n    }\n\n    if (isNaN(referredPointsNum) || referredPointsNum < 0) {\n      alert('추천받은 사람 포인트를 올바르게 입력해주세요.');\n      return;\n    }\n\n    try {\n      setSaving(true);\n      setError(null);\n\n      const updatedSettings = await dbManager.referralPoint.updateReferralPointSettings(editingId, {\n        referrerPoints: referrerPointsNum,\n        referredPoints: referredPointsNum,\n      });\n\n      if (updatedSettings) {\n        setSettings(prev => prev.map(s => \n          s.id === editingId ? updatedSettings : s\n        ));\n\n        handleCancelEdit();\n        console.log('추천 포인트 설정 수정됨:', updatedSettings);\n      } else {\n        throw new Error('수정 실패');\n      }\n    } catch (err) {\n      console.error('추천 포인트 설정 수정 실패:', err);\n      setError('추천 포인트 설정 수정에 실패했습니다.');\n    } finally {\n      setSaving(false);\n    }\n  };\n\n  const handleCancelEdit = () => {\n    setEditingId(null);\n    setEditReferrerPoints('');\n    setEditReferredPoints('');\n    setError(null);\n  };\n\n  const handleCancel = () => {\n    setSelectedBranchId('');\n    setReferrerPoints('');\n    setReferredPoints('');\n    setIsAdding(false);\n    setEditingId(null);\n    setEditReferrerPoints('');\n    setEditReferredPoints('');\n    setError(null);\n  };\n\n  if (loading) {\n    return (\n      <Container>\n        <LoadingState>데이터를 불러오는 중...</LoadingState>\n      </Container>\n    );\n  }\n\n  const availableBranches = getAvailableBranches();\n\n  return (\n    <Container>\n      {error && (\n        <ErrorState>\n          {error}\n          <RefreshButton onClick={loadData}>\n            다시 시도\n          </RefreshButton>\n        </ErrorState>\n      )}\n\n      {isAdding && (\n        <AddForm>\n          <FormRow>\n            <Select\n              value={selectedBranchId}\n              onChange={(e) => setSelectedBranchId(e.target.value)}\n              disabled={saving}\n              autoFocus\n            >\n              <option value=\"\">지점 선택 (필수)</option>\n              {availableBranches.map(branch => (\n                <option key={branch.id} value={branch.id}>\n                  {branch.name}\n                </option>\n              ))}\n            </Select>\n          </FormRow>\n          <FormRow>\n            <Input\n              type=\"number\"\n              placeholder=\"추천한 사람 포인트\"\n              value={referrerPoints}\n              onChange={(e) => setReferrerPoints(e.target.value)}\n              disabled={saving}\n              min=\"0\"\n            />\n            <Input\n              type=\"number\"\n              placeholder=\"추천받은 사람 포인트\"\n              value={referredPoints}\n              onChange={(e) => setReferredPoints(e.target.value)}\n              disabled={saving}\n              min=\"0\"\n            />\n          </FormRow>\n          <FormButtons>\n            <SaveButton onClick={handleAddSettings} disabled={saving}>\n              {saving ? '저장 중...' : '저장'}\n            </SaveButton>\n            <CancelButton onClick={handleCancel} disabled={saving}>\n              취소\n            </CancelButton>\n          </FormButtons>\n        </AddForm>\n      )}\n\n      {!isAdding && canAdd() && (\n        <AddButton \n          onClick={() => {\n            if (availableBranches.length === 0) {\n              alert('추가 가능한 지점이 없습니다. 모든 지점에 이미 설정이 등록되어 있습니다.');\n              return;\n            }\n            setIsAdding(true);\n            setEditingId(null);\n          }} \n          disabled={loading || editingId !== null || availableBranches.length === 0}\n        >\n          + 추천 포인트 설정 추가\n        </AddButton>\n      )}\n\n      <Table>\n        <TableHeader>\n          추천 포인트 설정\n          <TableStats>총 {settings.length}개 설정</TableStats>\n        </TableHeader>\n        {settings.length === 0 ? (\n          <EmptyState>등록된 추천 포인트 설정이 없습니다.</EmptyState>\n        ) : (\n          settings.map((setting) => (\n            <TableRow \n              key={setting.id}\n              style={editingId === setting.id ? {\n                flexDirection: 'column',\n                alignItems: 'stretch',\n                padding: '16px',\n                backgroundColor: `${AppColors.primary}05`\n              } : {}}\n            >\n              {editingId === setting.id ? (\n                // 수정 모드\n                <div style={{ \n                  width: '100%', \n                  display: 'flex', \n                  flexDirection: 'column', \n                  gap: '12px',\n                  padding: '8px 0'\n                }}>\n                  <div style={{ \n                    fontWeight: 500, \n                    marginBottom: '8px',\n                    color: AppColors.onSurface \n                  }}>\n                    {setting.branchName}\n                  </div>\n                  <div style={{ display: 'flex', gap: '8px' }}>\n                    <Input\n                      type=\"number\"\n                      value={editReferrerPoints}\n                      onChange={(e) => setEditReferrerPoints(e.target.value)}\n                      disabled={saving}\n                      placeholder=\"추천한 사람 포인트\"\n                      min=\"0\"\n                    />\n                    <Input\n                      type=\"number\"\n                      value={editReferredPoints}\n                      onChange={(e) => setEditReferredPoints(e.target.value)}\n                      disabled={saving}\n                      placeholder=\"추천받은 사람 포인트\"\n                      min=\"0\"\n                    />\n                  </div>\n                  <div style={{ \n                    display: 'flex', \n                    gap: '8px', \n                    justifyContent: 'flex-end',\n                    marginTop: '4px'\n                  }}>\n                    <SaveButton \n                      onClick={handleUpdateSettings} \n                      disabled={saving}\n                      style={{ \n                        padding: '6px 16px', \n                        fontSize: '13px',\n                        minWidth: '60px'\n                      }}\n                    >\n                      {saving ? '저장 중...' : '저장'}\n                    </SaveButton>\n                    <CancelButton \n                      onClick={handleCancelEdit} \n                      disabled={saving}\n                      style={{ \n                        padding: '6px 16px', \n                        fontSize: '13px',\n                        minWidth: '60px'\n                      }}\n                    >\n                      취소\n                    </CancelButton>\n                  </div>\n                </div>\n              ) : (\n                // 보기 모드\n                <>\n                  <SettingInfo>\n                    <BranchName>{setting.branchName}</BranchName>\n                    <PointDetails>\n                      <PointItem>\n                        <PointLabel>추천한 사람:</PointLabel>\n                        <PointValue>{setting.referrerPoints.toLocaleString()}P</PointValue>\n                      </PointItem>\n                      <PointItem>\n                        <PointLabel>추천받은 사람:</PointLabel>\n                        <PointValue>{setting.referredPoints.toLocaleString()}P</PointValue>\n                      </PointItem>\n                    </PointDetails>\n                  </SettingInfo>\n                  <ActionButtons>\n                    {canEdit(setting) && (\n                      <EditButton \n                        onClick={() => handleEditSettings(setting)}\n                        disabled={saving || editingId !== null}\n                      >\n                        수정\n                      </EditButton>\n                    )}\n                    {canDelete(setting) && (\n                      <DeleteButton \n                        onClick={() => handleDeleteSettings(setting.id)}\n                        disabled={saving || editingId !== null}\n                      >\n                        삭제\n                      </DeleteButton>\n                    )}\n                  </ActionButtons>\n                </>\n              )}\n            </TableRow>\n          ))\n        )}\n      </Table>\n    </Container>\n  );\n};\n\nexport default ReferralPointTable;\n"
        }
    ]
}